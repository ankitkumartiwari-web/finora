[build]
  command = "npm run build"
  publish = "dist"

[env.production]
  environment = {}

[context.production.environment]
  NODE_ENV = "production"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
